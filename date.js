



/**
 * Declaramos uma variavel chamada agora, que vai receber a instancia do objeto Date.
 * Após isso utilizando o metodo .toLocaleDateString("PT-BR") , exibimos a data no padrão brasileiro
 * 
 */


let agora = new Date();


console.log(agora.toLocaleDateString("PT-BR"));